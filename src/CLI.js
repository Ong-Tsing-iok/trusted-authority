// Get all global attribute
// Add global attribute
    // Check if already exist. 
    // Check if place left (if 'None' exist)
    // 'EOF' and 'None' are not allowed
// Change a global attribute (Warning! Files with the original attribute will have the changed attribute)

// Get user attribute
// add user attribute (can be used like add 普通 機密)
    // Will check if exist in global attributes
// remove user attribute (can be used like remove 普通 機密)